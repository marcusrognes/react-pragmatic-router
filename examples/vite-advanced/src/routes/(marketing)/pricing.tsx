export default function Pricing() {
	return (
		<section>
			<h2>Pricing</h2>
			<p>
				Lives at <code>routes/(marketing)/pricing.tsx</code>. The
				<code>(marketing)</code> folder is a route group and does not
				appear in the URL.
			</p>
		</section>
	);
}
